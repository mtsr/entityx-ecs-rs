extern crate test;

extern crate ecs;

use ecs::{
    Entity,
    Control,
    EntityManager,
    System,
    SystemManager,
    TupAppend, // required for components macro
    ComponentDatastructure,
};

#[test]
fn tests_directory_works() {
    assert!(true);
}